/**
 * Una expresión de función flecha es una alternativa compacta a una
 * expresión de función tradicional, pero es limitada y no
 * se puede utilizar en todas las situaciones.
 */

// function sumar(a, b) {
//     return a + b;
// }

const sumar = (a, b) => a + b

console.log(sumar(5, 10));