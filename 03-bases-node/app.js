const mutiplication = (number) => {
    for (let i = 1; i <= 10; i++) {
        console.log(`${number} x ${i} = ${number * i}`);
    }
}

const base = 5

console.clear();
console.log('========================');
console.log(`   Tabla del: ${base}   `);
console.log('========================');

mutiplication(2);